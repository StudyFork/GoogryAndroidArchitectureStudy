<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="itemViewModel"
            type="org.study.kotlin.androidarchitecturestudy.api.model.TickerModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/textview_item_market"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:gravity="center_vertical|start"
            android:paddingStart="12dp"
            android:textSize="20dp"
            android:layout_weight="0.4"
            tools:text="@{String.valueOf(itemViewModel.market)}"/>

        <TextView
            android:id="@+id/textview_item_trade_price"
            tools:text="@{String.valueOf(itemViewModel.tradePrice)}"
            style="@style/CusTextViewTextView" />

        <TextView
            android:id="@+id/textview_item_change_rate"
            tools:text="@{String.valueOf(itemViewModel.changeRate)}"
            style="@style/CusTextViewTextView" />

        <TextView
            android:id="@+id/textview_item_acc_trade_price_24h"
            tools:text="@{String.valueOf(itemViewModel.accTradePrice24h)}"
            style="@style/CusTextViewTextView" />

    </LinearLayout>
</layout>
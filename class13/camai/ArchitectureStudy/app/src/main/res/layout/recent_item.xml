<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="recentItem"
            type="com.camai.archtecherstudy.data.source.local.room.RecentSearchName" />

        <variable
            name="recentPresenter"
            type="com.camai.archtecherstudy.ui.rencentdialog.RecentMoviePresenter" />
    </data>

    <LinearLayout
        android:id="@+id/li_recent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/txt_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginHorizontal="24dp"
            android:fontFamily="sans-serif-black"
            android:onClick="@{()->recentPresenter.setClickData(recentItem.movieName)}"
            android:paddingTop="8dp"
            android:text="@{recentItem.movieName}"
            android:textSize="18sp"
            android:textStyle="bold" />
    </LinearLayout>
</layout>